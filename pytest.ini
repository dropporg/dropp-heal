[pytest]
testpaths = tests
# Make the api package importable however pytest is invoked; the console
# script, unlike `python -m pytest`, does not add the working directory.
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning
